# v16b: v16a source/basis asymmetry + zeroed key<->query crossing gains.
# Ablation of the five post-hoc components that pull k_t and q_t back
# together: congruence_gain, compat_refine_gain, deflation_vector,
# q_orthogonal_term, extra_deflation_vector. Isolates whether the
# post-hoc machinery is the neutralizer that kept v16a at cosine ~0.91.
# Layer on top of base.yaml + key_value_memory.yaml + local.yaml.

run:
  variant: stateful_v6_delta_memory_v16b
  run_id_prefix: v16b-no-cross
  generations: 18
  elite_top_k: 3

evolution:
  population_size: 48
