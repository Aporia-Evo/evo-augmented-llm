# v16a: architectural key/query asymmetry at the source.
# Overrides run.variant to select the new sub-variant; no new genes, no new
# genome dimensions. Strict A/B against v15f (stateful_v6_delta_memory).
# Layer on top of base.yaml + key_value_memory.yaml + local.yaml.

run:
  variant: stateful_v6_delta_memory_v16a
  run_id_prefix: v16a-asym-source
  generations: 18
  elite_top_k: 3

evolution:
  population_size: 48
